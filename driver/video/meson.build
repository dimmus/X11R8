subdir('amdgpu')
# subdir('armsoc') # conflicting with amdgpu
subdir('dummy')
subdir('fbdev')
# subdir('intel')   # TODO: finish build
subdir('nouveau')
# subdir('vbox')    # TODO: need "VboxVideo.h" (need dep_vbox?)

# TODO: add ati_drv.so, qxl_drv.so, radeon_drv.so, vesa_drv.so, vmware_drv.so